function getStrings() {
    const firstString = String(prompt('Введіть 1 рядок'));
    const secontString = String(prompt('Введіть 2 рядок'));
    const thirdString = String(prompt('Введіть 3 рядок'));

    return alert(`${thirdString} ${firstString} ${secontString}`);
}
